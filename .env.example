# Optional — required only for the Live judge tab and --live examples.
# Mint a key at https://console.typesafe.ai/keys
# Never commit this file as .env
TYPESAFE_API_KEY=

# Defaults
TYPESAFE_BASE_URL=https://api.typesafe.ai
TYPESAFE_MODEL=jev-latest
HOST=127.0.0.1
PORT=7872
